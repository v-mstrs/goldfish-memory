{
  "name": "goldfish-dexie",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:base": "tsc && vite build && vite build -c vite.content.config.ts && vite build -c vite.background.config.ts",
    "build": "npm run build:base && node scripts/build-dist.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "@types/webextension-polyfill": "^0.12.4",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@types/chrome": "^0.1.32",
    "@types/firefox-webext-browser": "^143.0.0",
    "dexie": "^4.2.1",
    "webextension-polyfill": "^0.12.0"
  }
}
